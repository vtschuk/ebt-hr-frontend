import {FilepickerDirective} from './filepicker.directive';

describe('AppFilepickerDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new FilepickerDirective();
    expect(directive).toBeTruthy();
  });
});
